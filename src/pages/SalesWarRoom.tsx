import { useEffect, useState } from "react";
import { useParams } from "react-router";
import { salesWarRoomApi } from "../lib/salesWarRoomApi";

const stages = ["New Lead","Contacted","Cold","Warm","Hot / Very Potential","Hold","Meeting Scheduled","Meeting Held","Negotiation / Closing","Won","Lost / Dead"];
const ar: Record<string,string> = {
  "New Lead":"ليد جديد","Contacted":"تم التواصل","Cold":"Cold","Warm":"Warm","Hot / Very Potential":"Hot / قوي جدًا","Hold":"Hold","Meeting Scheduled":"ميعاد متحدد","Meeting Held":"تم الاجتماع","Negotiation / Closing":"تفاوض / Closing","Won":"مكسب","Lost / Dead":"خسارة / Dead"
};

type PipelineView = "kanban"|"queue"|"tabs"|"table"|"calendar"|"funnel";
type LeadDraft = {
  client_name:string;
  phone:string;
  budget:string;
  expected_value:string;
  stage:string;
  next_action:string;
  next_action_date:string;
  next_action_time:string;
  next_action_trigger:string;
  notes:string;
};

const emptyLead:LeadDraft={client_name:"",phone:"",budget:"",expected_value:"",stage:"New Lead",next_action:"",next_action_date:"",next_action_time:"",next_action_trigger:"",notes:""};

function phoneForCall(phone:string){return String(phone||"").replace(/[^\d+]/g,"")}
function phoneForWhatsApp(phone:string){let digits=String(phone||"").replace(/\D/g,"");if(digits.startsWith("00"))digits=digits.slice(2);if(/^01\d{9}$/.test(digits))digits=`20${digits.slice(1)}`;return digits}
function todayLocal(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}
function millionsToEgp(value:string){if(!String(value||"").trim())return null;const n=Number(value);return Number.isFinite(n)&&n>=0?Math.round(n*1_000_000):null}
function valueToMillions(value:any){const n=Number(value||0);if(!n)return "";return String(n/1_000_000)}
function formatEgp(value:any){const n=Number(value||0);if(n>=1_000_000_000){const v=n/1_000_000_000;return `EGP ${Number.isInteger(v)?v:v.toFixed(1)}B`}if(n>=1_000_000){const v=n/1_000_000;return `EGP ${Number.isInteger(v)?v:v.toFixed(1)}M`}if(n>=1_000){const v=n/1_000;return `EGP ${Number.isInteger(v)?v:v.toFixed(1)}K`}return `EGP ${n.toLocaleString()}`}

export default function SalesWarRoom(){
  const { slug = "" } = useParams();
  const sessionKey=`warRoomAgentToken:${slug}`;
  const viewKey=`warRoomPipelineView:${slug}`;
  const ownerToken=localStorage.getItem("warRoomAdminToken")||"";
  const isOwnerView=Boolean(ownerToken);
  const [lang,setLang]=useState<"en"|"ar">((localStorage.getItem("warRoomLang") as "en"|"ar")||"en");
  const [token,setToken]=useState(()=>localStorage.getItem(sessionKey)||"");
  const [password,setPassword]=useState("");
  const [data,setData]=useState<any>(null);
  const [leaders,setLeaders]=useState<any>(null);
  const [loading,setLoading]=useState(Boolean(token||ownerToken));
  const [err,setErr]=useState("");
  const [lead,setLead]=useState<LeadDraft>({...emptyLead});
  const [editingId,setEditingId]=useState<string>("");
  const [editLead,setEditLead]=useState<LeadDraft>({...emptyLead});
  const [pipelineView,setPipelineView]=useState<PipelineView>(()=>(localStorage.getItem(viewKey) as PipelineView)||"kanban");
  const [stageTab,setStageTab]=useState("Warm");
  const t=(en:string,arr:string)=>lang==="ar"?arr:en;
  const clearSession=()=>{localStorage.removeItem(sessionKey);setToken("");setData(null);setLeaders(null)};

  const load=async()=>{
    if(!token){setLoading(false);return}
    try{
      setLoading(true);
      const [agentData,leaderData]=await Promise.all([salesWarRoomApi.getAgent(slug,token),salesWarRoomApi.getLeaders(token)]);
      setData(agentData);setLeaders(leaderData);setErr("");
    }catch(e:any){
      if(e.message==="unauthorized"){clearSession();setErr(t("Session expired. Enter your password again.","انتهت جلسة الدخول. اكتب الباسورد مرة تانية."))}
      else setErr(e.message);
    }finally{setLoading(false)}
  };

  async function bootstrapOwnerAccess(){
    if(token||!ownerToken)return;
    try{setLoading(true);setErr("");const r=await salesWarRoomApi.adminAgentAccess(ownerToken,slug);localStorage.setItem(sessionKey,r.token);setToken(r.token)}
    catch(e:any){if(e.message==="unauthorized")localStorage.removeItem("warRoomAdminToken");setErr(e.message==="unauthorized"?t("Owner session expired. Sign in again from Owner Dashboard.","جلسة الـOwner انتهت. ادخل من Owner Dashboard مرة تانية."):e.message)}
    finally{setLoading(false)}
  }

  async function login(){
    try{setLoading(true);setErr("");const r=await salesWarRoomApi.agentLogin(slug,password);localStorage.setItem(sessionKey,r.token);setToken(r.token);setPassword("")}
    catch(e:any){setErr(e.message==="invalid_credentials"?t("Wrong password","الباسورد غير صحيح"):e.message)}finally{setLoading(false)}
  }

  useEffect(()=>{document.title="Sales War Room";const m=document.createElement("meta");m.name="robots";m.content="noindex,nofollow,noarchive";document.head.appendChild(m);return()=>m.remove()},[]);
  useEffect(()=>{if(token)void load();else if(ownerToken)void bootstrapOwnerAccess();else setLoading(false)},[slug,token]);
  useEffect(()=>{localStorage.setItem("warRoomLang",lang);document.documentElement.dir=lang==="ar"?"rtl":"ltr"},[lang]);
  useEffect(()=>{localStorage.setItem(viewKey,pipelineView)},[pipelineView,viewKey]);

  const score=data?.score||{};
  const pipeline=data?.pipeline||[];
  const matches=[1,2,3,4].map(i=>({i,calls:Number(score[`match${i}_calls`]||0),status:score[`match${i}_status`]||"open"}));
  const todayWins=matches.filter(m=>m.status==="win").length;
  const todayLosses=matches.filter(m=>m.status==="loss").length;
  const todayCalls=matches.reduce((a,m)=>a+m.calls,0);
  const warm=pipeline.filter((l:any)=>l.stage==="Warm").length;
  const hot=pipeline.filter((l:any)=>l.stage==="Hot / Very Potential").length;
  const expectedSales=pipeline.filter((l:any)=>["Warm","Hot / Very Potential"].includes(l.stage)).reduce((sum:number,l:any)=>sum+Number(l.expected_value||0),0);
  const lostSales=pipeline.filter((l:any)=>l.stage==="Lost / Dead").reduce((sum:number,l:any)=>sum+Number(l.expected_value||0),0);
  const weekly=(()=>{const rows=data?.weekScores||[];let wins=0,losses=0,calls=0,potential=0,meetings=0,sales=0;rows.forEach((r:any)=>{for(let i=1;i<=4;i++){calls+=Number(r[`match${i}_calls`]||0);if(r[`match${i}_status`]==="win")wins++;if(r[`match${i}_status`]==="loss")losses++}potential+=Number(r.potential_cases||0);meetings+=Number(r.meetings_scheduled||0);sales+=Number(r.sales_volume||0)});return{wins,losses,calls,potential,meetings,sales,total:wins+losses}})();

  async function changeCalls(i:number,delta:number){const m=matches[i-1];if(m.status!=="open")return;for(let x=1;x<i;x++)if(matches[x-1].status==="open"){alert(t(`Finish Match ${x} first.`,`اقفل Match ${x} الأول.`));return}const calls=Math.max(0,Math.min(50,m.calls+delta));const body:any={slug,[`match${i}_calls`]:calls};if(calls===50)body[`match${i}_status`]="win";await salesWarRoomApi.patchScore(token,body);void load()}
  async function finish(i:number){const m=matches[i-1];if(m.status!=="open")return;if(m.calls<50&&!confirm(t(`Record Match ${i} as LOSS at ${m.calls}/50?`,`نسجل Match ${i} خسارة عند ${m.calls}/50؟`)))return;await salesWarRoomApi.patchScore(token,{slug,[`match${i}_calls`]:m.calls,[`match${i}_status`]:m.calls===50?"win":"loss"});void load()}
  async function potential(d:number){await salesWarRoomApi.patchScore(token,{slug,potential_cases:Math.max(0,Number(score.potential_cases||0)+d)});void load()}

  function warmReady(x:LeadDraft){return Boolean(x.next_action.trim()&&(x.next_action_date||x.next_action_trigger.trim()))}
  function expectedValid(x:LeadDraft){return !x.expected_value.trim()||(Number.isFinite(Number(x.expected_value))&&Number(x.expected_value)>=0)}
  async function addLead(){if(!lead.client_name.trim())return alert(t("Client name required","اسم العميل مطلوب"));if(!expectedValid(lead))return alert(t("Expected Sale must be a valid positive number in millions.","Expected Sale لازم يكون رقم صحيح بالمليون."));if(lead.stage==="Warm"&&!warmReady(lead))return alert(t("Warm requires Next Action plus a Date or Trigger.","Warm لازم يكون له Next Action ومعاه Date أو Trigger."));await salesWarRoomApi.addLead(token,{slug,...lead,expected_value:millionsToEgp(lead.expected_value),next_action_date:lead.next_action_date||null,next_action_time:lead.next_action_date?(lead.next_action_time||"09:00"):null});setLead({...emptyLead});void load()}
  function startEdit(x:any,stageOverride?:string){setEditingId(x.id);setEditLead({client_name:x.client_name||"",phone:x.phone||"",budget:x.budget||"",expected_value:valueToMillions(x.expected_value),stage:stageOverride||x.stage||"New Lead",next_action:x.next_action||"",next_action_date:x.next_action_date||"",next_action_time:String(x.next_action_time||"").slice(0,5),next_action_trigger:x.next_action_trigger||"",notes:x.notes||""})}
  async function saveEdit(){if(!editingId)return;if(!editLead.client_name.trim())return alert(t("Client name required","اسم العميل مطلوب"));if(!expectedValid(editLead))return alert(t("Expected Sale must be a valid positive number in millions.","Expected Sale لازم يكون رقم صحيح بالمليون."));if(editLead.stage==="Warm"&&!warmReady(editLead))return alert(t("Warm requires Next Action plus a Date or Trigger.","Warm لازم يكون له Next Action ومعاه Date أو Trigger."));await salesWarRoomApi.updateLead(token,{id:editingId,...editLead,expected_value:millionsToEgp(editLead.expected_value),next_action_date:editLead.next_action_date||null,next_action_time:editLead.next_action_date?(editLead.next_action_time||"09:00"):null});setEditingId("");setEditLead({...emptyLead});void load()}
  async function moveLead(id:string,stage:string){const x=pipeline.find((p:any)=>p.id===id);if(stage==="Warm"&&(!x.next_action||(!x.next_action_date&&!x.next_action_trigger))){startEdit(x,"Warm");return}await salesWarRoomApi.updateLead(token,{id,stage});void load()}

  if(!token&&ownerToken&&loading)return <main className="grid min-h-screen place-items-center bg-slate-950 text-white">{t("Opening agent dashboard as Owner…","جاري فتح داشبورد الـAgent كـOwner…")}</main>;
  if(!token)return <main className="grid min-h-screen place-items-center bg-slate-950 p-4 text-white" dir={lang==="ar"?"rtl":"ltr"}><div className="w-full max-w-sm rounded-3xl border border-white/10 bg-white/5 p-6 shadow-2xl"><div className="mb-6 flex items-center justify-between"><div><div className="text-xs font-black tracking-[.18em] text-slate-400">TYCOONS SALES WAR ROOM</div><h1 className="mt-1 text-2xl font-black">{t("Agent Access","دخول الـAgent")}</h1></div><button onClick={()=>setLang(lang==="ar"?"en":"ar")} className="rounded-full border border-white/20 px-3 py-2 text-xs font-black">{lang==="ar"?"EN":"عربي"}</button></div><p className="mb-4 text-sm text-slate-400">{t("Enter your personal password to open your scoreboard.","اكتب الباسورد الخاص بيك علشان تفتح الـScoreboard.")}</p><input type="password" autoFocus value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==="Enter"&&login()} placeholder={t("Password","الباسورد")} className="w-full rounded-xl border border-white/10 bg-white/10 p-3 outline-none"/><button disabled={loading||!password} onClick={login} className="mt-3 w-full rounded-xl bg-white p-3 font-black text-slate-950 disabled:opacity-50">{loading?t("Checking…","جاري التحقق…"):t("Open War Room","افتح غرفة العمليات")}</button>{err&&<div className="mt-3 text-sm text-red-300">{err}</div>}</div></main>;
  if(loading&&!data)return <main className="grid min-h-screen place-items-center bg-slate-950 text-white">{t("Loading War Room…","جاري تحميل غرفة العمليات…")}</main>;
  if(err&&!data)return <main className="grid min-h-screen place-items-center bg-slate-950 p-4 text-white"><div className="text-center"><div>{t("Dashboard unavailable","الداشبورد غير متاحة")}</div><div className="mt-2 text-sm text-red-300">{err}</div><button onClick={clearSession} className="mt-4 rounded-xl bg-white px-4 py-2 font-black text-slate-950">{t("Sign in again","دخول من جديد")}</button></div></main>;

  const name=lang==="ar"?data.agent.name_ar:data.agent.name_en;
  const leaderNames=(items:any[]|undefined)=>items?.length?items.map(x=>lang==="ar"?x.name_ar:x.name_en).join(" + "):t("No leader yet","لسه مفيش متصدر");

  return <main className="min-h-screen bg-[#f3f5f7] text-[#111317]" dir={lang==="ar"?"rtl":"ltr"}>
    <div className="mx-auto max-w-[1600px] p-3 md:p-5">
      <header className="mb-4 flex items-center justify-between"><div><div className="text-xs font-black tracking-[.2em] text-slate-500">TYCOONS SALES WAR ROOM</div><h1 className="text-2xl font-black">{name}</h1></div><div className="flex gap-2"><button onClick={()=>setLang(lang==="ar"?"en":"ar")} className="rounded-full border bg-white px-4 py-2 text-sm font-black shadow-sm">{lang==="ar"?"EN":"عربي"}</button>{isOwnerView?<button onClick={()=>window.location.href="/sales-war-room/admin"} className="rounded-full bg-slate-950 px-4 py-2 text-sm font-black text-white">{t("Back to Owner","الرجوع للـOwner")}</button>:<button onClick={clearSession} className="rounded-full bg-slate-950 px-4 py-2 text-sm font-black text-white">{t("Logout","خروج")}</button>}</div></header>

      <section className="grid gap-4 rounded-3xl bg-slate-950 p-4 text-white shadow-xl md:grid-cols-[1.15fr_1fr] md:p-6">
        <div className="flex flex-col justify-between gap-6"><div><div className="text-xs font-black tracking-[.18em] text-slate-400">{t("TODAY · WHAT DO I NEED TO WIN NEXT?","اليوم · إيه اللي ناقصني علشان أكسب الـMatch الجاي؟")}</div><div className="mt-4 flex flex-wrap items-end gap-8"><div><div className="text-xs text-slate-400">{t("CALL TARGET","هدف المكالمات")}</div><div className="text-5xl font-black">{todayCalls}<span className="text-2xl text-slate-500">/200</span></div></div><div><div className="text-xs text-slate-400">{t("TODAY'S RECORD","نتيجة اليوم")}</div><div className="text-4xl font-black"><span className="text-emerald-400">{todayWins}W</span> – <span className="text-rose-400">{todayLosses}L</span></div></div></div></div><div className="rounded-2xl border border-white/10 bg-white/5 p-3 text-sm font-bold">{t("No carryover. Every Match starts 0–0.","مفيش تعويض. كل Match بيبدأ 0–0.")}</div></div>
        <div className="grid grid-cols-2 gap-2">{matches.map(m=><div key={m.i} className="rounded-2xl bg-white p-3 text-slate-950"><div className="flex items-center justify-between text-xs font-black"><span>MATCH {m.i}</span><span className={`rounded-full px-2 py-1 ${m.status==="win"?"bg-emerald-100 text-emerald-700":m.status==="loss"?"bg-rose-100 text-rose-700":"bg-slate-100"}`}>{m.status==="open"?t("LIVE","شغال"):m.status.toUpperCase()}</span></div><div className="mt-2 text-3xl font-black">{m.calls}<span className="text-sm text-slate-400"> / 50</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-slate-950" style={{width:`${m.calls*2}%`}}/></div><div className="mt-3 grid grid-cols-3 gap-1"><button onClick={()=>changeCalls(m.i,5)} className="rounded-lg border px-2 py-2 text-xs font-black">+5</button><button onClick={()=>changeCalls(m.i,25)} className="rounded-lg border px-2 py-2 text-xs font-black">+25</button><button onClick={()=>finish(m.i)} className="rounded-lg bg-slate-950 px-2 py-2 text-xs font-black text-white">{t("Finish","اقفل")}</button></div></div>)}</div>
      </section>

      <section className="mt-4 grid gap-3 md:grid-cols-[1.45fr_.75fr]">
        <div className="rounded-3xl bg-[#10261f] p-5 text-white shadow-sm"><div className="text-xs font-black tracking-[.14em] text-emerald-200">{name.toUpperCase()} — {t("EXPECTED SALES","المبيعات المتوقعة")}</div><div className="mt-2 text-4xl font-black md:text-5xl">{formatEgp(expectedSales)}</div><div className="mt-2 text-xs font-bold text-emerald-100/70">{t("Warm + Hot pipeline value","قيمة الـWarm + Hot Pipeline")}</div></div>
        <div className="rounded-3xl border border-red-200 bg-red-50 p-5 text-red-900 shadow-sm"><div className="text-xs font-black tracking-[.14em] text-red-500">{t("LOST SALES","المبيعات المفقودة")}</div><div className="mt-2 text-4xl font-black">{formatEgp(lostSales)}</div><div className="mt-2 text-xs font-bold text-red-600/70">{t("Value moved to Lost / Dead","قيمة الـLeads اللي اتحولت Lost / Dead")}</div></div>
      </section>

      <section className="mt-4 rounded-3xl border bg-white p-4 shadow-sm">
        <div className="mb-3"><div className="text-xs font-black tracking-[.15em] text-slate-400">{t("TEAM MOTIVATION BOARD","لوحة تحفيز الفريق")}</div><h2 className="text-lg font-black">{t("Who is leading right now?","مين متصدر دلوقتي؟")}</h2></div>
        <div className="grid gap-3 md:grid-cols-2"><LeaderPeriod title={t("TODAY","النهاردة")} calls={leaderNames(leaders?.daily?.calls)} hot={leaderNames(leaders?.daily?.hot)} t={t}/><LeaderPeriod title={t("THIS WEEK","الأسبوع ده")} calls={leaderNames(leaders?.weekly?.calls)} hot={leaderNames(leaders?.weekly?.hot)} t={t}/></div>
      </section>

      <section className="mt-4 grid gap-3 md:grid-cols-4"><Kpi title={t("Warm Pipeline","Warm Pipeline")} value={`${warm} / 10`} sub={warm<10?t("RED ALERT","إنذار أحمر"):t("Protected","آمن")} danger={warm<10}/><Kpi title={t("Potential Today","Potential النهاردة")} value={`${score.potential_cases||0} / ~5`} actions={<><button onClick={()=>potential(1)} className="rounded-lg border px-2 py-1">+1</button><button onClick={()=>potential(-1)} className="rounded-lg border px-2 py-1">−1</button></>}/><Kpi title={t("Weekly Meetings","Meetings الأسبوع")} value={`${weekly.meetings} / 8`} sub={`${t("Hot","Hot")}: ${hot}`}/><Kpi title={t("Weekly Match Score","نتيجة الأسبوع")} value={`${weekly.wins}W / ${weekly.total}`} sub={`${weekly.total?Math.round(weekly.wins/weekly.total*100):0}% ${t("win rate","نسبة فوز")}`}/></section>
      {warm<10&&<div className="mt-3 rounded-2xl border-2 border-red-500 bg-red-50 p-4 text-red-800"><b>🚨 {t("PIPELINE RED ALERT","إنذار أحمر للـPIPELINE")}</b><div className="mt-1 text-sm">{t("Warm is below 10. Pipeline generation outranks side projects.","الـWarm أقل من 10. بناء الـPipeline له الأولوية قبل أي Side Projects.")}</div></div>}

      <section className="mt-4 rounded-3xl border bg-white p-4 shadow-sm">
        <div className="mb-4 flex flex-wrap items-start justify-between gap-3">
          <div><h2 className="text-lg font-black">{t("Pipeline View","عرض الـPipeline")}</h2><p className="text-xs text-slate-500">{t("Choose the view that fits what you need to do right now.","اختار طريقة العرض المناسبة للي محتاج تعمله دلوقتي.")}</p></div>
          <label className="flex items-center gap-2 text-xs font-black text-slate-500"><span>{t("VIEW","العرض")}</span><select value={pipelineView} onChange={e=>setPipelineView(e.target.value as PipelineView)} className="rounded-xl border bg-white px-3 py-2 text-sm font-black text-slate-950"><option value="kanban">Kanban</option><option value="queue">{t("Action Queue","قائمة المتابعة")}</option><option value="tabs">{t("Stage Tabs","مراحل")}</option><option value="table">{t("Table","جدول")}</option><option value="calendar">{t("Calendar","تقويم")}</option><option value="funnel">Funnel</option></select></label>
        </div>

        {editingId&&<div className="mb-4 max-w-xl rounded-2xl bg-slate-50 p-2"><LeadEditor draft={editLead} setDraft={setEditLead} stages={stages} ar={ar} lang={lang} t={t} onSave={saveEdit} onCancel={()=>{setEditingId("");setEditLead({...emptyLead})}}/></div>}

        {pipelineView==="kanban"&&<KanbanView pipeline={pipeline} stages={stages} ar={ar} lang={lang} t={t} startEdit={startEdit} moveLead={moveLead}/>} 
        {pipelineView==="queue"&&<ActionQueueView pipeline={pipeline} stages={stages} ar={ar} lang={lang} t={t} startEdit={startEdit} moveLead={moveLead}/>} 
        {pipelineView==="tabs"&&<StageTabsView pipeline={pipeline} stages={stages} ar={ar} lang={lang} t={t} stageTab={stageTab} setStageTab={setStageTab} startEdit={startEdit} moveLead={moveLead}/>} 
        {pipelineView==="table"&&<TableView pipeline={pipeline} ar={ar} lang={lang} t={t} startEdit={startEdit}/>} 
        {pipelineView==="calendar"&&<CalendarView pipeline={pipeline} ar={ar} lang={lang} t={t} startEdit={startEdit}/>} 
        {pipelineView==="funnel"&&<FunnelView pipeline={pipeline} stages={stages} ar={ar} lang={lang} t={t}/>} 

        <div className="mt-5 border-t pt-4"><h3 className="mb-3 font-black">+ {t("Add Lead","إضافة Lead")}</h3><div className="grid gap-2 md:grid-cols-5"><input value={lead.client_name} onChange={e=>setLead({...lead,client_name:e.target.value})} placeholder={t("Client name","اسم العميل")} className="rounded-xl border p-3"/><input value={lead.phone} onChange={e=>setLead({...lead,phone:e.target.value})} placeholder={t("Phone","الموبايل")} className="rounded-xl border p-3"/><input value={lead.budget} onChange={e=>setLead({...lead,budget:e.target.value})} placeholder={t("Budget","الميزانية")} className="rounded-xl border p-3"/><input type="number" min="0" step="0.1" value={lead.expected_value} onChange={e=>setLead({...lead,expected_value:e.target.value})} placeholder={t("Expected Sale (M EGP)","Expected Sale بالمليون")} className="rounded-xl border p-3"/><select value={lead.stage} onChange={e=>setLead({...lead,stage:e.target.value})} className="rounded-xl border p-3">{stages.map(s=><option key={s} value={s}>{lang==="ar"?ar[s]:s}</option>)}</select><input value={lead.next_action} onChange={e=>setLead({...lead,next_action:e.target.value})} placeholder={t("Next action","الخطوة الجاية")} className="rounded-xl border p-3 md:col-span-2"/><input type="date" value={lead.next_action_date} min={todayLocal()} onChange={e=>setLead({...lead,next_action_date:e.target.value,next_action_time:e.target.value?(lead.next_action_time||"09:00"):""})} className="rounded-xl border p-3"/><input type="time" value={lead.next_action_time} disabled={!lead.next_action_date} onChange={e=>setLead({...lead,next_action_time:e.target.value})} aria-label={t("Follow-up time","وقت المتابعة")} className="rounded-xl border p-3 disabled:bg-slate-100 disabled:text-slate-400"/><input value={lead.next_action_trigger} onChange={e=>setLead({...lead,next_action_trigger:e.target.value})} placeholder={t("Trigger (optional)","Trigger اختياري")} className="rounded-xl border p-3"/><button onClick={addLead} className="rounded-xl bg-slate-950 p-3 font-black text-white">{t("Add to Pipeline","ضيف للـPipeline")}</button><textarea value={lead.notes} onChange={e=>setLead({...lead,notes:e.target.value})} placeholder={t("Full feedback / client context / competing offers","الفيدباك كامل / سياق العميل / العروض المنافسة")} className="min-h-[96px] rounded-xl border p-3 md:col-span-5"/></div></div>
      </section>

      <section className="mt-4 grid gap-3 md:grid-cols-2"><div className="rounded-3xl border bg-white p-4"><h2 className="font-black">{t("Weekly Review","مراجعة الأسبوع")}</h2><div className="mt-3 grid grid-cols-2 gap-2 text-sm"><Metric label={t("Calls","Calls")} value={weekly.calls}/><Metric label={t("Potential","Potential")} value={weekly.potential}/><Metric label={t("Wins","Wins")} value={weekly.wins}/><Metric label={t("Losses","Losses")} value={weekly.losses}/><Metric label={t("Meetings","Meetings")} value={weekly.meetings}/><Metric label={t("Sales","Sales")} value={`EGP ${Number(weekly.sales).toLocaleString()}`}/></div></div><div className="rounded-3xl border bg-white p-4"><h2 className="font-black">{t("Operating Rules","قواعد التشغيل")}</h2><ol className="mt-3 space-y-2 text-sm text-slate-700"><li><b>1.</b> {t("No Carryover","مفيش تعويض")}</li><li><b>2.</b> {t("Every Match Starts 0–0","كل Match بيبدأ 0–0")}</li><li><b>3.</b> {t("Warm Pipeline Must Stay Above 10","Warm Pipeline لازم يفضل فوق 10")}</li><li><b>4.</b> {t("Hot Cases Do Not Stop Pipeline Building","Hot Cases ما توقفش بناء Pipeline")}</li><li><b>5.</b> {t("First Priority = Pipeline","الأولوية الأولى = Pipeline")}</li></ol></div></section>
    </div>
  </main>
}

function LeaderPeriod({title,calls,hot,t}:any){return <div className="rounded-2xl border bg-slate-50 p-4"><div className="mb-3 text-xs font-black tracking-[.12em] text-slate-500">{title}</div><div className="grid gap-2 sm:grid-cols-2"><div className="rounded-xl bg-white p-3"><div className="text-[10px] font-black text-slate-400">🏃 {t("HIGHEST CALLS","أعلى CALLS")}</div><div className="mt-1 text-lg font-black">{calls}</div></div><div className="rounded-xl bg-white p-3"><div className="text-[10px] font-black text-slate-400">🔥 {t("MOST HOT LEADS","أعلى HOT LEADS")}</div><div className="mt-1 text-lg font-black">{hot}</div></div></div></div>}

function KanbanView({pipeline,stages,ar,lang,t,startEdit,moveLead}:any){return <div className="overflow-x-auto pb-2"><div className="grid min-w-max grid-flow-col auto-cols-[290px] gap-2">{stages.map((stage:string)=><div key={stage} className="min-h-[300px] rounded-2xl border bg-slate-50 p-2"><div className="mb-2 flex items-center justify-between text-xs font-black"><span>{lang==="ar"?ar[stage]:stage}</span><span className="rounded-full bg-white px-2 py-1">{pipeline.filter((x:any)=>x.stage===stage).length}</span></div>{pipeline.filter((x:any)=>x.stage===stage).map((x:any)=><LeadCard key={x.id} x={x} stage={stage} stages={stages} ar={ar} lang={lang} t={t} onEdit={()=>startEdit(x)} onMove={(s:string)=>moveLead(x.id,s)}/>)}</div>)}</div></div>}

function ActionQueueView({pipeline,stages,ar,lang,t,startEdit,moveLead}:any){const today=todayLocal();const priority=(x:any)=>x.next_action_date&&x.next_action_date<today?0:x.next_action_date===today?1:x.stage==="Hot / Very Potential"?2:x.stage==="Warm"?3:4;const rows=[...pipeline].filter((x:any)=>!["Won","Lost / Dead"].includes(x.stage)).sort((a:any,b:any)=>priority(a)-priority(b)||(a.next_action_date||"9999").localeCompare(b.next_action_date||"9999"));return <div className="space-y-2">{rows.length?rows.map((x:any)=>{const overdue=x.next_action_date&&x.next_action_date<today;const due=x.next_action_date===today;return <div key={x.id} className={`grid gap-3 rounded-2xl border p-3 md:grid-cols-[1.1fr_.8fr_1.5fr_auto] md:items-center ${overdue?"border-red-300 bg-red-50":due?"border-amber-300 bg-amber-50":"bg-slate-50"}`}><div><div className="font-black">{x.client_name}</div><div className="mt-1 text-xs font-bold text-slate-500">{lang==="ar"?ar[x.stage]:x.stage}</div>{x.expected_value?<div className="mt-1 text-xs font-black text-emerald-700">{t("Expected","متوقع")}: {formatEgp(x.expected_value)}</div>:null}</div><div className="text-xs"><div className="font-black">{overdue?`🚨 ${t("OVERDUE","متأخر")}`:due?`⏰ ${t("DUE TODAY","النهاردة")}`:x.next_action_date||t("No date","بدون تاريخ")}</div>{x.phone&&<a href={`tel:${phoneForCall(x.phone)}`} className="mt-1 block underline" dir="ltr">{x.phone}</a>}</div><div className="text-xs leading-5"><b>{t("Next Action","الخطوة الجاية")}:</b> {x.next_action||"—"}{x.notes&&<div className="mt-1 text-slate-500 line-clamp-2">{x.notes}</div>}</div><div className="flex gap-2"><button onClick={()=>startEdit(x)} className="rounded-lg border bg-white px-3 py-2 text-xs font-black">{t("Edit","تعديل")}</button><select value={x.stage} onChange={e=>moveLead(x.id,e.target.value)} className="rounded-lg border bg-white px-2 py-2 text-xs font-bold">{stages.map((s:string)=><option key={s} value={s}>{lang==="ar"?ar[s]:s}</option>)}</select></div></div>}):<EmptyState text={t("No active leads yet.","مفيش Leads نشطة لسه.")}/>}</div>}

function StageTabsView({pipeline,stages,ar,lang,t,stageTab,setStageTab,startEdit,moveLead}:any){const rows=pipeline.filter((x:any)=>x.stage===stageTab);return <div><div className="mb-3 flex gap-2 overflow-x-auto pb-1">{stages.map((s:string)=><button key={s} onClick={()=>setStageTab(s)} className={`whitespace-nowrap rounded-full border px-3 py-2 text-xs font-black ${stageTab===s?"bg-slate-950 text-white":"bg-white"}`}>{lang==="ar"?ar[s]:s} · {pipeline.filter((x:any)=>x.stage===s).length}</button>)}</div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{rows.length?rows.map((x:any)=><LeadCard key={x.id} x={x} stage={x.stage} stages={stages} ar={ar} lang={lang} t={t} onEdit={()=>startEdit(x)} onMove={(s:string)=>moveLead(x.id,s)}/>):<EmptyState text={t("No leads in this stage.","مفيش Leads في المرحلة دي.")}/>}</div></div>}

function TableView({pipeline,ar,lang,t,startEdit}:any){return <div className="overflow-x-auto"><table className="w-full min-w-[1050px] text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="p-3 text-start">{t("Client","العميل")}</th><th className="p-3 text-start">{t("Phone","الموبايل")}</th><th className="p-3 text-start">{t("Stage","المرحلة")}</th><th className="p-3 text-start">{t("Expected Sale","المبيعات المتوقعة")}</th><th className="p-3 text-start">{t("Follow-up Date","تاريخ المتابعة")}</th><th className="p-3 text-start">{t("Next Action","الخطوة الجاية")}</th><th className="p-3 text-start">{t("Feedback","الفيدباك")}</th><th className="p-3"></th></tr></thead><tbody>{pipeline.map((x:any)=><tr key={x.id} className="border-t"><td className="p-3 font-black">{x.client_name}</td><td className="p-3" dir="ltr">{x.phone||"—"}</td><td className="p-3 font-bold">{lang==="ar"?ar[x.stage]:x.stage}</td><td className={`p-3 font-black ${x.stage==="Lost / Dead"?"text-red-600":"text-emerald-700"}`}>{x.expected_value?formatEgp(x.expected_value):"—"}</td><td className="p-3" dir="ltr">{x.next_action_date?`${x.next_action_date}${x.next_action_time?` · ${String(x.next_action_time).slice(0,5)}`:""}`:"—"}</td><td className="max-w-[260px] p-3 text-xs">{x.next_action||"—"}</td><td className="max-w-[320px] p-3 text-xs"><div className="max-h-20 overflow-y-auto whitespace-pre-wrap">{x.notes||"—"}</div></td><td className="p-3"><button onClick={()=>startEdit(x)} className="rounded-lg border px-3 py-2 text-xs font-black">{t("Edit","تعديل")}</button></td></tr>)}</tbody></table>{!pipeline.length&&<EmptyState text={t("No leads yet.","مفيش Leads لسه.")}/>}</div>}

function CalendarView({pipeline,ar,lang,t,startEdit}:any){const dated=[...pipeline].filter((x:any)=>x.next_action_date).sort((a:any,b:any)=>a.next_action_date.localeCompare(b.next_action_date));const groups=dated.reduce((acc:Record<string,any[]>,x:any)=>{(acc[x.next_action_date]||=[]).push(x);return acc},{});return <div className="space-y-3">{Object.keys(groups).length?Object.entries(groups).map(([date,rows])=>{const overdue=date<todayLocal(),due=date===todayLocal();return <div key={date} className={`rounded-2xl border p-3 ${overdue?"border-red-300 bg-red-50":due?"border-amber-300 bg-amber-50":"bg-slate-50"}`}><div className="mb-2 flex items-center justify-between"><div className="font-black" dir="ltr">{date}</div><div className="text-xs font-black">{overdue?`🚨 ${t("OVERDUE","متأخر")}`:due?`⏰ ${t("TODAY","النهاردة")}`:""}</div></div><div className="grid gap-2 md:grid-cols-2">{(rows as any[]).map((x:any)=><button key={x.id} onClick={()=>startEdit(x)} className="rounded-xl border bg-white p-3 text-start"><div className="font-black">{x.client_name}</div><div className="mt-1 text-xs font-bold text-slate-500">{lang==="ar"?ar[x.stage]:x.stage}</div>{x.expected_value?<div className="mt-1 text-xs font-black text-emerald-700">{formatEgp(x.expected_value)}</div>:null}{x.next_action_time?<div className="mt-1 text-[10px] font-black text-slate-500">⏰ {String(x.next_action_time).slice(0,5)}</div>:null}<div className="mt-2 text-xs">{x.next_action||"—"}</div></button>)}</div></div>}):<EmptyState text={t("No dated follow-ups yet.","مفيش Follow-ups بتاريخ لسه.")}/>}</div>}

function FunnelView({pipeline,stages,ar,lang,t}:any){const max=Math.max(1,...stages.map((s:string)=>pipeline.filter((x:any)=>x.stage===s).length));return <div className="space-y-2">{stages.map((s:string)=>{const count=pipeline.filter((x:any)=>x.stage===s).length;const width=Math.max(count?8:2,(count/max)*100);return <div key={s} className="grid grid-cols-[150px_1fr_40px] items-center gap-3"><div className="text-xs font-black">{lang==="ar"?ar[s]:s}</div><div className="h-9 overflow-hidden rounded-lg bg-slate-100"><div className="flex h-full items-center rounded-lg bg-slate-900 px-3 text-xs font-black text-white" style={{width:`${width}%`}}></div></div><div className="text-center text-sm font-black">{count}</div></div>})}<div className="mt-3 text-xs text-slate-500">{t("Funnel is for a quick pipeline-health view; use another view for follow-up work.","الـFunnel للمراجعة السريعة لصحة الـPipeline؛ استخدم View تانية للشغل على الـFollow-ups.")}</div></div>}

function LeadCard({x,stage,stages,ar,lang,t,onEdit,onMove}:any){const waPhone=phoneForWhatsApp(x.phone);const callPhone=phoneForCall(x.phone);const dueToday=x.next_action_date===todayLocal();const lost=stage==="Lost / Dead";return <div className={`mb-2 rounded-xl border bg-white p-3 shadow-sm ${stage==="Hot / Very Potential"?"border-red-300":stage==="Warm"?"border-amber-300":lost?"border-red-200":""}`}><div className="flex items-start justify-between gap-2"><div className="font-black">{x.client_name}</div><button onClick={onEdit} className="rounded-lg border px-2 py-1 text-[10px] font-black">✏️ {t("Edit","تعديل")}</button></div>{x.expected_value?<div className={`mt-2 rounded-lg p-2 text-xs font-black ${lost?"bg-red-50 text-red-700":"bg-emerald-50 text-emerald-800"}`}><span>{lost?t("LOST SALE","مبيعات مفقودة"):t("EXPECTED SALE","مبيعات متوقعة")}</span><div className="mt-1 text-base">{formatEgp(x.expected_value)}</div></div>:null}{x.phone?<div className="mt-2 flex flex-col gap-2"><a href={`tel:${callPhone}`} dir="ltr" className="break-all text-sm font-black text-slate-900 underline decoration-slate-300 underline-offset-2">📞 {x.phone}</a><div className="grid grid-cols-2 gap-2"><a href={`tel:${callPhone}`} className="rounded-lg border px-2 py-2 text-center text-xs font-black">{t("Call","اتصال")}</a>{waPhone?<a href={`https://web.whatsapp.com/send?phone=${waPhone}`} target="_blank" rel="noreferrer" className="rounded-lg bg-emerald-600 px-2 py-2 text-center text-xs font-black text-white">WhatsApp</a>:null}</div></div>:<div className="mt-2 rounded-lg bg-amber-50 p-2 text-xs font-bold text-amber-800">{t("No phone number saved","رقم التليفون مش متسجل")}</div>}{x.budget?<div className="mt-2 text-xs text-slate-500"><b>{t("Budget","الميزانية")}:</b> {x.budget}</div>:null}{(x.next_action||x.next_action_date||x.next_action_trigger)&&<div className={`mt-2 rounded-lg p-2 text-xs leading-5 ${dueToday?"border border-amber-300 bg-amber-50":"bg-slate-50"}`}><b>{t("Next Action","الخطوة الجاية")}: </b><span className="whitespace-pre-wrap break-words">{x.next_action||"—"}</span>{x.next_action_date?<><br/><b>{t("Date","التاريخ")}: </b><span dir="ltr">{x.next_action_date}{x.next_action_time?` · ${String(x.next_action_time).slice(0,5)}`:""}</span>{dueToday?<span className="ms-2 font-black text-amber-700">⏰ {t("DUE TODAY","ميعاده النهاردة")}</span>:null}</>:null}{x.next_action_trigger?<><br/><b>{t("Trigger","Trigger")}: </b><span className="whitespace-pre-wrap break-words">{x.next_action_trigger}</span></>:null}</div>}{x.notes?<div className="mt-2 rounded-lg border border-slate-200 bg-white p-2 text-xs leading-5 text-slate-700"><b className="block text-slate-950">{t("Feedback / Notes","الفيدباك / الملاحظات")}</b><div className="mt-1 whitespace-pre-wrap break-words">{x.notes}</div></div>:null}<select value={x.stage} onChange={e=>onMove(e.target.value)} className="mt-2 w-full rounded-lg border p-2 text-xs font-bold">{stages.map((s:string)=><option key={s} value={s}>{lang==="ar"?ar[s]:s}</option>)}</select></div>}

function LeadEditor({draft,setDraft,stages,ar,lang,t,onSave,onCancel}:any){return <div className="mb-2 rounded-xl border-2 border-slate-400 bg-white p-3 shadow-sm"><div className="mb-2 text-xs font-black">✏️ {t("EDIT LEAD","تعديل الـLEAD")}</div><div className="grid gap-2"><input value={draft.client_name} onChange={e=>setDraft({...draft,client_name:e.target.value})} placeholder={t("Client name","اسم العميل")} className="rounded-lg border p-2 text-xs"/><input value={draft.phone} onChange={e=>setDraft({...draft,phone:e.target.value})} placeholder={t("Phone","الموبايل")} className="rounded-lg border p-2 text-xs"/><input value={draft.budget} onChange={e=>setDraft({...draft,budget:e.target.value})} placeholder={t("Budget","الميزانية")} className="rounded-lg border p-2 text-xs"/><label className="text-[10px] font-black text-slate-500">{t("EXPECTED SALE (M EGP)","EXPECTED SALE بالمليون")}<input type="number" min="0" step="0.1" value={draft.expected_value} onChange={e=>setDraft({...draft,expected_value:e.target.value})} placeholder="25" className="mt-1 w-full rounded-lg border p-2 text-xs"/></label><select value={draft.stage} onChange={e=>setDraft({...draft,stage:e.target.value})} className="rounded-lg border p-2 text-xs font-bold">{stages.map((s:string)=><option key={s} value={s}>{lang==="ar"?ar[s]:s}</option>)}</select><input value={draft.next_action} onChange={e=>setDraft({...draft,next_action:e.target.value})} placeholder={t("Next action","الخطوة الجاية")} className="rounded-lg border p-2 text-xs"/><label className="text-[10px] font-black text-slate-500">{t("FOLLOW-UP DATE","تاريخ الـFOLLOW-UP")}<input type="date" value={draft.next_action_date} onChange={e=>setDraft({...draft,next_action_date:e.target.value,next_action_time:e.target.value?(draft.next_action_time||"09:00"):""})} className="mt-1 w-full rounded-lg border p-2 text-xs"/></label><label className="text-[10px] font-black text-slate-500">{t("FOLLOW-UP TIME","وقت الـFOLLOW-UP")}<input type="time" value={draft.next_action_time} disabled={!draft.next_action_date} onChange={e=>setDraft({...draft,next_action_time:e.target.value})} className="mt-1 w-full rounded-lg border p-2 text-xs disabled:bg-slate-100"/></label><input value={draft.next_action_trigger} onChange={e=>setDraft({...draft,next_action_trigger:e.target.value})} placeholder={t("Trigger (optional)","Trigger اختياري")} className="rounded-lg border p-2 text-xs"/><textarea value={draft.notes} onChange={e=>setDraft({...draft,notes:e.target.value})} placeholder={t("Full feedback / notes","الفيدباك كامل / الملاحظات")} className="min-h-[90px] rounded-lg border p-2 text-xs"/><div className="grid grid-cols-2 gap-2"><button onClick={onCancel} className="rounded-lg border p-2 text-xs font-black">{t("Cancel","إلغاء")}</button><button onClick={onSave} className="rounded-lg bg-slate-950 p-2 text-xs font-black text-white">{t("Save Changes","حفظ التعديل")}</button></div></div></div>}

function EmptyState({text}:any){return <div className="rounded-2xl border border-dashed bg-slate-50 p-8 text-center text-sm font-bold text-slate-400">{text}</div>}
function Kpi({title,value,sub,actions,danger=false}:any){return <div className={`rounded-2xl border bg-white p-4 shadow-sm ${danger?"border-red-400":""}`}><div className="text-xs font-black text-slate-500">{title}</div><div className={`mt-1 text-3xl font-black ${danger?"text-red-600":""}`}>{value}</div>{sub&&<div className="mt-1 text-xs font-bold text-slate-500">{sub}</div>}{actions&&<div className="mt-3 flex gap-2 text-xs">{actions}</div>}</div>}
function Metric({label,value}:any){return <div className="rounded-xl bg-slate-50 p-3"><div className="text-xs text-slate-500">{label}</div><div className="mt-1 font-black">{value}</div></div>}
